version: "3.9"

services:
  buscacursos-scraper:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - scraper_data:/data
    restart: unless-stopped

volumes:
  scraper_data:
